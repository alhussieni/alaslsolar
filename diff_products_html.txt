--- orig/alaslsolar-main/products.html	2026-08-02 02:42:53.000000000 +0000
+++ project/alaslsolar-main/products.html	2026-08-02 19:10:44.412010116 +0000
@@ -497,6 +497,62 @@
       align-items: center;
       gap: 8px;
     }
+
+    /* ── Multi-level collapsible filter tree ── */
+    .ftree-search{position:relative;margin-bottom:10px}
+    .ftree-search input{width:100%;padding:9px 34px 9px 12px;border:1.5px solid var(--line);
+      border-radius:var(--radius);font-family:inherit;font-size:13px;background:var(--bg);color:var(--ink)}
+    .ftree-search input:focus{outline:none;border-color:var(--brand)}
+    .ftree-search svg{position:absolute;right:11px;top:50%;transform:translateY(-50%);opacity:.5;pointer-events:none}
+    [dir="rtl"] .ftree-search svg{right:auto;left:11px}
+
+    .ftree-scroll{max-height:520px;overflow-y:auto;margin:0 -4px;padding:0 4px}
+    .ftree-scroll::-webkit-scrollbar{width:6px}
+    .ftree-scroll::-webkit-scrollbar-thumb{background:var(--line);border-radius:99px}
+
+    .ftree-heading{margin:14px 0 4px;font-size:11px;font-weight:700;color:var(--muted);
+      text-transform:uppercase;letter-spacing:.02em}
+    .ftree-node:first-child .ftree-heading{margin-top:0}
+
+    .ftree-row{display:flex;align-items:center;gap:8px;padding:7px 6px;border-radius:8px;
+      cursor:pointer;font-size:13.5px;color:var(--ink);user-select:none}
+    .ftree-row:hover{background:var(--brand-soft)}
+    .ftree-row.selected{background:var(--brand-soft);color:var(--brand-dark);font-weight:800}
+
+    .ftree-toggle{width:16px;height:16px;flex:0 0 16px;display:flex;align-items:center;justify-content:center;
+      color:var(--muted);transition:transform .15s;cursor:pointer}
+    .ftree-toggle.no-children{visibility:hidden}
+    .ftree-node.open > .ftree-row > .ftree-toggle{transform:rotate(90deg)}
+
+    .ftree-box{width:16px;height:16px;flex:0 0 16px;border:1.5px solid var(--line);border-radius:4px;
+      display:flex;align-items:center;justify-content:center;background:#fff;transition:.12s}
+    .ftree-box svg{width:10px;height:10px;opacity:0;transition:.12s}
+    .ftree-row.checked .ftree-box{background:var(--brand);border-color:var(--brand)}
+    .ftree-row.checked .ftree-box svg{opacity:1;stroke:#fff}
+    .ftree-row.indeterminate .ftree-box{background:var(--brand);border-color:var(--brand);opacity:.55}
+
+    .ftree-label{flex:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
+    .ftree-count{font-size:11px;color:var(--muted);font-weight:700;background:var(--bg);
+      border-radius:999px;padding:1px 7px;flex:0 0 auto}
+    .ftree-row.selected .ftree-count{background:#fff;color:var(--brand-dark)}
+
+    .ftree-children{margin-inline-start:24px;border-inline-start:1.5px dashed var(--line);
+      padding-inline-start:8px;overflow:hidden;max-height:0;transition:max-height .18s ease}
+    .ftree-node.open > .ftree-children{max-height:3000px}
+    .ftree-lvl3 .ftree-row{color:var(--muted);font-size:13px}
+    .ftree-lvl3 .ftree-row.selected{color:var(--brand-dark)}
+    .ftree-empty{padding:14px 6px;color:var(--muted);font-size:13px;text-align:center}
+
+    /* Active filter chips, shown above the product grid */
+    .active-chips{display:flex;flex-wrap:wrap;align-items:center;gap:8px;margin-bottom:var(--space-3)}
+    .active-chip{display:flex;align-items:center;gap:6px;background:var(--brand-soft);color:var(--brand-dark);
+      border-radius:999px;padding:6px 8px 6px 14px;font-size:12.5px;font-weight:700}
+    [dir="rtl"] .active-chip{padding:6px 14px 6px 8px}
+    .active-chip button{background:#fff;border:none;width:18px;height:18px;border-radius:50%;cursor:pointer;
+      color:var(--brand-dark);font-size:11px;line-height:1;display:flex;align-items:center;justify-content:center}
+    .active-chip-clear{border:1.5px solid var(--line);background:#fff;border-radius:999px;padding:6px 14px;
+      font-size:12.5px;font-weight:700;color:var(--muted);cursor:pointer}
+    .active-chip-clear:hover{border-color:var(--brand);color:var(--brand-dark)}
     .prod-price-field {
       flex: 1;
       display: flex;
@@ -555,7 +611,7 @@
     .prod-btn-reset:hover { border-color: var(--brand); color: var(--brand-dark); }
 
     /* Product grid */
-    .prod-grid-wrap { grid-column: 1; }
+    .prod-grid-wrap { grid-column: 1; grid-row: 2; }
     .prod-grid {
       display: grid;
       grid-template-columns: repeat(auto-fill, minmax(230px, 1fr));
@@ -672,11 +728,46 @@
       padding: 2px 6px;
       margin-bottom: 4px;
     }
+    .new-badge {
+      display: inline-block;
+      font-size: 10px;
+      font-weight: 700;
+      background: var(--forest);
+      color: #fff;
+      border-radius: 4px;
+      padding: 2px 6px;
+      margin-bottom: 4px;
+      margin-inline-end: 4px;
+    }
     .prod-card.out-of-stock .prod-card-media { filter: grayscale(60%); opacity: .75; }
 
+    /* Grid / List view toggle */
+    .prod-view-toggle { display: flex; gap: 4px; border: 1.5px solid var(--line); border-radius: var(--radius); padding: 3px; background: #fff; }
+    .prod-view-btn { display: flex; align-items: center; justify-content: center; width: 30px; height: 30px; border: none; border-radius: 7px; background: transparent; color: var(--muted); cursor: pointer; }
+    .prod-view-btn.active { background: var(--brand-soft); color: var(--brand-dark); }
+    .prod-view-btn svg { width: 15px; height: 15px; }
+
+    .prod-grid.is-list { display: flex; flex-direction: column; gap: var(--space-2); }
+    .prod-grid.is-list .prod-card { flex-direction: row; align-items: stretch; }
+    .prod-grid.is-list .prod-card-media { width: 150px; flex: 0 0 150px; aspect-ratio: auto; }
+    .prod-grid.is-list .prod-card-body { flex: 1; }
+    @media (max-width: 560px) {
+      .prod-grid.is-list .prod-card { flex-direction: column; }
+      .prod-grid.is-list .prod-card-media { width: 100%; flex-basis: auto; }
+    }
+
+    /* Pagination */
+    .prod-pagination { grid-column: 1; display: flex; align-items: center; justify-content: center; gap: 6px; margin-top: var(--space-4); flex-wrap: wrap; }
+    .prod-page-btn { min-width: 34px; height: 34px; padding: 0 8px; border: 1.5px solid var(--line); border-radius: var(--radius); background: #fff; color: var(--ink); font-family: inherit; font-size: 13px; font-weight: 700; cursor: pointer; }
+    .prod-page-btn:hover:not(:disabled) { border-color: var(--brand); color: var(--brand-dark); }
+    .prod-page-btn.active { background: var(--brand); border-color: var(--brand); color: #fff; }
+    .prod-page-btn:disabled { opacity: .4; cursor: default; }
+    .prod-page-ellipsis { color: var(--muted); padding: 0 4px; }
+
     /* Loading / empty states */
     .prod-loading {
       grid-column: 1;
+      grid-row: 2;
       text-align: center;
       padding: var(--space-5);
       color: var(--muted);
@@ -774,6 +865,14 @@
 
       <!-- Toolbar -->
       <div class="prod-toolbar">
+        <div class="prod-view-toggle" role="group" aria-label="نمط العرض">
+          <button type="button" class="prod-view-btn active" id="viewGridBtn" aria-label="عرض شبكي">
+            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="3" width="7" height="7" rx="1.5"/><rect x="3" y="14" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/></svg>
+          </button>
+          <button type="button" class="prod-view-btn" id="viewListBtn" aria-label="عرض قائمة">
+            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="4" y1="6" x2="20" y2="6"/><line x1="4" y1="12" x2="20" y2="12"/><line x1="4" y1="18" x2="20" y2="18"/></svg>
+          </button>
+        </div>
         <select class="prod-sort-select" id="prodSort" aria-label="ترتيب المنتجات">
           <option value="newest" data-i18n="prod_sort_newest">الأحدث</option>
         </select>
@@ -788,7 +887,9 @@
 
       <!-- Product grid -->
       <div class="prod-grid-wrap">
+        <div class="active-chips" id="activeChips" hidden></div>
         <div class="prod-grid" id="prodGrid" hidden></div>
+        <div class="prod-pagination" id="prodPagination" hidden></div>
 
         <!-- Empty state -->
         <div class="prod-empty" id="prodEmpty" hidden>
@@ -801,84 +902,16 @@
       <!-- Sidebar -->
       <aside class="prod-sidebar">
 
-        <!-- Category filter -->
+        <!-- Category filter tree (built dynamically in JS from live product + category data) -->
         <div class="prod-sidebar-section">
           <p class="prod-sidebar-label" data-i18n="prod_sidebar_category">الفئة</p>
-          <div class="prod-radio-list" id="catRadioList" role="radiogroup" aria-label="تصفية الفئة">
-            <label class="prod-radio-item active" data-cat="all">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_all">جميع المنتجات</span></span>
-              <input type="radio" name="prodCat" value="all" checked>
-            </label>
-            <label class="prod-radio-item" data-cat="inverters">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_inverters">⚡ إنفرترات</span></span>
-              <input type="radio" name="prodCat" value="inverters">
-            </label>
-            <label class="prod-radio-item" data-cat="panels">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_panels">☀️ ألواح شمسية</span></span>
-              <input type="radio" name="prodCat" value="panels">
-            </label>
-            <label class="prod-radio-item" data-cat="structures">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_structures">🏗️ هياكل</span></span>
-              <input type="radio" name="prodCat" value="structures">
-            </label>
-            <label class="prod-radio-item" data-cat="cables">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_cables">🔶 كابلات</span></span>
-              <input type="radio" name="prodCat" value="cables">
-            </label>
-            <label class="prod-radio-item" data-cat="accessories">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_accessories">🔌 إكسسوارات</span></span>
-              <input type="radio" name="prodCat" value="accessories">
-            </label>
-            <label class="prod-radio-item" data-cat="combiners">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_combiners">📦 صناديق تجميع</span></span>
-              <input type="radio" name="prodCat" value="combiners">
-            </label>
-            <label class="prod-radio-item" data-cat="batteries">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_batteries">🔋 بطاريات</span></span>
-              <input type="radio" name="prodCat" value="batteries">
-            </label>
-            <label class="prod-radio-item" data-cat="offgrid">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_offgrid">🔆 إنفرتر أوف جريد</span></span>
-              <input type="radio" name="prodCat" value="offgrid">
-            </label>
-            <label class="prod-radio-item" data-cat="well_motors">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_well_motors">🛠️ موتورات آبار</span></span>
-              <input type="radio" name="prodCat" value="well_motors">
-            </label>
-            <label class="prod-radio-item" data-cat="pumps">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_pumps">🌊 طلمبات ومضخات</span></span>
-              <input type="radio" name="prodCat" value="pumps">
-            </label>
-            <label class="prod-radio-item" data-cat="pipes">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_pipes">🧵 مواسير</span></span>
-              <input type="radio" name="prodCat" value="pipes">
-            </label>
-            <label class="prod-radio-item" data-cat="street_lights">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_street_lights">💡 إنارة شوارع شمسية</span></span>
-              <input type="radio" name="prodCat" value="street_lights">
-            </label>
-            <label class="prod-radio-item" data-cat="flood_lights">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_flood_lights">🔦 كشافات شمسية</span></span>
-              <input type="radio" name="prodCat" value="flood_lights">
-            </label>
-            <label class="prod-radio-item" data-cat="garden_lights">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_garden_lights">🪴 إضاءة حدائق شمسية</span></span>
-              <input type="radio" name="prodCat" value="garden_lights">
-            </label>
-            <label class="prod-radio-item" data-cat="solar_kits">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_solar_kits">🧰 أنظمة طاقة شمسية منزلية</span></span>
-              <input type="radio" name="prodCat" value="solar_kits">
-            </label>
-            <label class="prod-radio-item" data-cat="solar_safety">
-              <span class="prod-radio-text"><span data-i18n="prod_cat_solar_safety">🚧 سلامة الطرق الشمسية</span></span>
-              <input type="radio" name="prodCat" value="solar_safety">
-            </label>
+          <div class="ftree-search">
+            <input type="text" id="ftreeSearch" placeholder="ابحث في التصنيفات أو الماركات…">
+            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2"><circle cx="11" cy="11" r="7"/><path d="M21 21l-4-4"/></svg>
           </div>
+          <div class="ftree-scroll" id="filterTree"></div>
         </div>
 
-        <!-- Dynamic sub-filter (brand / power) — shown depending on selected category -->
-        <div class="prod-sidebar-section" id="dynamicSubFilter" hidden></div>
-
         <!-- Price filter -->
         <div class="prod-sidebar-section">
           <p class="prod-sidebar-label" data-i18n="prod_sidebar_price">السعر (ج.م)</p>
@@ -996,10 +1029,15 @@
     }
 
     // ── State ──
-    let allProducts = [];     // flat list of every product across categories, each tagged with .category
-    let currentCat = 'all';
-    let currentBrand = 'all'; // sub-filter: brand (inverters, panels)
-    let currentPower = 'all'; // sub-filter: power in KW (inverters only)
+    let allProducts = [];        // flat list of every product across categories, each tagged with .category
+    let filterTree = [];         // built once per load from allProducts + category_settings
+    let selectedFilters = new Set(); // node ids the user checked, e.g. "panels" / "panels::Longi" / "pumps::Ferat::5"
+    let nodeLabels = {};         // id -> human label, for the active-filter chips
+    let openNodes = new Set();   // ids currently expanded in the tree UI
+    let lastSectionOf = {};      // category -> main_section, cached so we can rebuild the tree on language change
+    let viewMode = 'grid';       // 'grid' | 'list'
+    let currentPage = 1;
+    const PAGE_SIZE = 24;
     let priceMin = null;
     let priceMax = null;
 
@@ -1051,13 +1089,23 @@
     }
 
     // ── Render a single product card ──
+    // A product counts as "new" for 30 days after it was added — driven by
+    // created_at, which already exists on every row, so no schema change needed.
+    function isNewProduct(createdAt) {
+      if (!createdAt) return false;
+      const days = (Date.now() - new Date(createdAt).getTime()) / 86400000;
+      return days >= 0 && days <= 30;
+    }
+    const NEW_BADGE_TEXT = { ar: 'جديد', en: 'New', es: 'Nuevo', zh: '新品' };
+
     function renderCard(p, cheapSet) {
       const icon = CAT_ICONS[p.category] || '📦';
       const isCheap = cheapSet && cheapSet.has(p.id || p);
       const outOfStock = p.in_stock === false;
-      const badge = outOfStock
+      const newBadge = isNewProduct(p.created_at) ? `<span class="new-badge">${NEW_BADGE_TEXT[getLang()] || NEW_BADGE_TEXT.ar}</span>` : '';
+      const badge = newBadge + (outOfStock
         ? `<span class="stock-badge">${u('outOfStock')}</span>`
-        : (isCheap ? `<span class="cheaper-badge">${u('cheaper')}</span>` : '');
+        : (isCheap ? `<span class="cheaper-badge">${u('cheaper')}</span>` : ''));
 
       const ds = p.datasheet_url
         ? `<span class="prod-card-ds" onclick="event.preventDefault();event.stopPropagation();window.open('${escapeHtml(p.datasheet_url)}','_blank','noopener')"><i class="fa fa-file-pdf-o"></i> PDF</span>`
@@ -1182,6 +1230,8 @@
         : `<span class="prod-fallback-icon">${icon}</span>`;
 
       const anyInStock = group.some(r => r.in_stock !== false);
+      const isNewFamily = group.some(r => isNewProduct(r.created_at));
+      const newBadge = isNewFamily ? `<span class="new-badge">${NEW_BADGE_TEXT[getLang()] || NEW_BADGE_TEXT.ar}</span>` : '';
       const prices = group.map(r => r.price);
       const min = fmt(Math.min(...prices));
       const max = fmt(Math.max(...prices));
@@ -1201,6 +1251,7 @@
             <div class="prod-card-meta"><span>${variantsLine}</span></div>
             <div class="prod-card-bottom">
               <div>
+                ${newBadge ? `<div>${newBadge}</div>` : ''}
                 <span class="prod-card-price" style="font-size:14px">${priceLine} <span class="prod-unit">${unitFor(p)}</span></span>
               </div>
             </div>
@@ -1208,7 +1259,34 @@
         </a>`;
     }
 
-    // ── Apply category + price filters and render grid ──
+    // ── Which product field holds the "power" tier for a given category's tree ──
+    function powerFieldFor(cat) {
+      if (cat === 'well_motors' || cat === 'pumps') return 'power_hp';
+      if (cat === 'inverters' || cat === 'offgrid') return 'power_kw';
+      if (cat === 'panels') return 'power_w';
+      return null;
+    }
+    function fmtPower(v) { return String(Number(v)); } // normalizes "5.50" / 5.5 / "5.5" to the same key
+
+    // Does this product satisfy at least one of the checked tree nodes?
+    // A node id is "category", "category::brand" or "category::brand::power" —
+    // checking a parent implicitly covers everything under it.
+    function productMatchesSelection(p) {
+      if (selectedFilters.size === 0) return true;
+      for (const id of selectedFilters) {
+        const parts = id.split('::');
+        if (parts[0] !== p.category) continue;
+        if (parts.length === 1) return true;
+        if (parts[1] !== (p.brand || '—')) continue;
+        if (parts.length === 2) return true;
+        const field = powerFieldFor(p.category);
+        const val = field && p[field] != null ? fmtPower(p[field]) : null;
+        if (val !== null && parts[2] === val) return true;
+      }
+      return false;
+    }
+
+    // ── Apply category-tree + price filters and render grid ──
     function renderGrid() {
       const grid = document.getElementById('prodGrid');
       const empty = document.getElementById('prodEmpty');
@@ -1216,12 +1294,7 @@
       if (!grid) return;
 
       let filtered = allProducts.filter(p => {
-        if (currentCat !== 'all' && p.category !== currentCat) return false;
-        if (currentBrand !== 'all' && p.brand !== currentBrand) return false;
-        if (currentPower !== 'all' && p.category === 'inverters') {
-          const { kw } = parseInvPower(p);
-          if (String(kw) !== String(currentPower)) return false;
-        }
+        if (!productMatchesSelection(p)) return false;
         if (priceMin !== null && p.price < priceMin) return false;
         if (priceMax !== null && p.price > priceMax) return false;
         return true;
@@ -1245,124 +1318,261 @@
         grid.hidden = true;
         grid.innerHTML = '';
         if (empty) empty.hidden = false;
+        const pagination = document.getElementById('prodPagination');
+        if (pagination) { pagination.hidden = true; pagination.innerHTML = ''; }
         return;
       }
 
       if (empty) empty.hidden = true;
       grid.hidden = false;
+      grid.classList.toggle('is-list', viewMode === 'list');
 
       // Group same-family variants (same category+brand+model) into ONE card
       // instead of one card per power/spec point — the family card links to
       // product-detail.html, which already has the power/variant picker.
       // Categories with mostly one-off products (single variant per family)
       // just render as before, one card per product.
-      grid.innerHTML = Array.from(groups.values())
+      const allCards = Array.from(groups.values());
+      const totalPages = Math.max(1, Math.ceil(allCards.length / PAGE_SIZE));
+      if (currentPage > totalPages) currentPage = totalPages;
+      const pageCards = allCards.slice((currentPage - 1) * PAGE_SIZE, currentPage * PAGE_SIZE);
+
+      grid.innerHTML = pageCards
         .map(group => group.length > 1 ? renderFamilyCard(group, cheapSet) : renderCard(group[0], cheapSet))
         .join('');
+
+      renderPagination(totalPages);
     }
 
-    // ── Category radio handling ──
-    function setActiveCategory(cat) {
-      currentCat = cat;
-      currentBrand = 'all';
-      currentPower = 'all';
-      document.querySelectorAll('.prod-radio-item').forEach(item => {
-        item.classList.toggle('active', item.dataset.cat === cat);
-      });
-      renderSubFilter();
+    function renderPagination(totalPages) {
+      const wrap = document.getElementById('prodPagination');
+      if (!wrap) return;
+      if (totalPages <= 1) { wrap.hidden = true; wrap.innerHTML = ''; return; }
+      wrap.hidden = false;
+
+      const pageBtn = (n, label, opts = {}) => `<button type="button" class="prod-page-btn${n === currentPage ? ' active' : ''}"
+        data-page="${n}" ${opts.disabled ? 'disabled' : ''} aria-label="صفحة ${label ?? n}">${label ?? n}</button>`;
+
+      let html = pageBtn(currentPage - 1, '‹', { disabled: currentPage === 1 });
+      const windowStart = Math.max(1, currentPage - 1);
+      const windowEnd = Math.min(totalPages, currentPage + 1);
+      if (windowStart > 1) { html += pageBtn(1); if (windowStart > 2) html += `<span class="prod-page-ellipsis">…</span>`; }
+      for (let n = windowStart; n <= windowEnd; n++) html += pageBtn(n);
+      if (windowEnd < totalPages) { if (windowEnd < totalPages - 1) html += `<span class="prod-page-ellipsis">…</span>`; html += pageBtn(totalPages); }
+      html += pageBtn(currentPage + 1, '›', { disabled: currentPage === totalPages });
+
+      wrap.innerHTML = html;
+    }
+
+    document.getElementById('prodPagination')?.addEventListener('click', (e) => {
+      const btn = e.target.closest('[data-page]');
+      if (!btn || btn.disabled) return;
+      currentPage = parseInt(btn.getAttribute('data-page'), 10);
+      renderGrid();
+      document.querySelector('.prod-toolbar')?.scrollIntoView({ behavior: 'smooth', block: 'start' });
+    });
+
+    function setViewMode(mode) {
+      viewMode = mode;
+      document.getElementById('viewGridBtn')?.classList.toggle('active', mode === 'grid');
+      document.getElementById('viewListBtn')?.classList.toggle('active', mode === 'list');
       renderGrid();
     }
+    document.getElementById('viewGridBtn')?.addEventListener('click', () => setViewMode('grid'));
+    document.getElementById('viewListBtn')?.addEventListener('click', () => setViewMode('list'));
 
-    // ── Sub-filter labels per language ──
-    const SUBFILTER_LABELS = {
-      brand: { ar: 'الماركة', en: 'Brand', es: 'Marca', zh: '品牌' },
-      power: { ar: 'القدرة (KW)', en: 'Power (KW)', es: 'Potencia (KW)', zh: '功率 (KW)' },
-      allBrands: { ar: 'كل الماركات', en: 'All Brands', es: 'Todas las marcas', zh: '全部品牌' },
-      allPowers: { ar: 'كل القدرات', en: 'All Powers', es: 'Todas las potencias', zh: '全部功率' },
+    // ── i18n lookup shared with the rest of the site (translations is defined in app.js) ──
+    function tr(key) {
+      const dict = (typeof translations !== 'undefined' && translations[getLang()]) || {};
+      return dict[key] || (typeof translations !== 'undefined' && translations.ar && translations.ar[key]) || key;
+    }
+
+    // Section grouping + per-section category order — mirrors the taxonomy
+    // agreed on for the products page (irrigation → inverters → panels →
+    // install components → standalone lighting → batteries).
+    const SECTIONS = [
+      { id: 'irrigation_wells',    i18n: 'prod_section_irrigation', cats: ['well_motors', 'pumps', 'pipes'] },
+      { id: 'inverters',           i18n: 'prod_section_inverters',  cats: ['inverters', 'offgrid', 'ongrid', 'hybrid'] },
+      { id: 'panels',              i18n: 'prod_section_panels',     cats: ['panels'] },
+      { id: 'install_components',  i18n: 'prod_section_install',    cats: ['structures', 'cables', 'combiners', 'accessories'] },
+      { id: 'standalone_lighting', i18n: 'prod_section_lighting',   cats: ['street_lights', 'flood_lights', 'garden_lights', 'solar_safety', 'solar_kits'] },
+      { id: 'batteries',           i18n: 'prod_section_batteries',  cats: ['batteries'] },
+    ];
+    const CAT_I18N_KEY = {
+      well_motors: 'prod_cat_well_motors', pumps: 'prod_cat_pumps', pipes: 'prod_cat_pipes',
+      inverters: 'prod_cat_inverters', offgrid: 'prod_cat_offgrid',
+      panels: 'prod_cat_panels', structures: 'prod_cat_structures', cables: 'prod_cat_cables',
+      combiners: 'prod_cat_combiners', accessories: 'prod_cat_accessories',
+      street_lights: 'prod_cat_street_lights', flood_lights: 'prod_cat_flood_lights',
+      garden_lights: 'prod_cat_garden_lights', solar_safety: 'prod_cat_solar_safety',
+      solar_kits: 'prod_cat_solar_kits', batteries: 'prod_cat_batteries',
     };
-    function sl(key) { return (SUBFILTER_LABELS[key] && SUBFILTER_LABELS[key][getLang()]) || SUBFILTER_LABELS[key].ar; }
+    // Categories deep enough (brand + power variants) to get a 3-level branch;
+    // everything else in the tree is a flat, childless leaf.
+    const BRAND_POWER_CATS = new Set(['well_motors', 'pumps', 'inverters', 'offgrid', 'panels']);
+    const POWER_UNIT_LABEL = { power_hp: 'HP', power_kw: 'KW', power_w: 'W' };
+
+    // ── Build the tree structure from the live product list + category_settings ──
+    // Runs once per data load (not per render) — cheap even at 1000+ products
+    // since it's a handful of GROUP BY-style passes over an in-memory array.
+    function buildFilterTree(sectionOf) {
+      nodeLabels = { all: tr('prod_cat_all') };
+      const presentCats = new Set(allProducts.map(p => p.category));
+
+      filterTree = SECTIONS.map(section => {
+        const cats = section.cats.filter(c => presentCats.has(c)); // hides empty/placeholder cats (e.g. ongrid, hybrid) automatically
+        if (!cats.length) return null;
+
+        const catNodes = cats.map(cat => {
+          const catProducts = allProducts.filter(p => p.category === cat);
+          const label = tr(CAT_I18N_KEY[cat] || cat);
+          nodeLabels[cat] = label;
+
+          let children;
+          if (BRAND_POWER_CATS.has(cat)) {
+            const byBrand = {};
+            catProducts.forEach(p => { (byBrand[p.brand || '—'] = byBrand[p.brand || '—'] || []).push(p); });
+            const field = powerFieldFor(cat);
+            const unit = POWER_UNIT_LABEL[field] || '';
+
+            children = Object.keys(byBrand).sort().map(brand => {
+              const brandProducts = byBrand[brand];
+              const brandId = `${cat}::${brand}`;
+              nodeLabels[brandId] = brand;
+
+              let powerChildren;
+              if (field) {
+                const byPower = {};
+                brandProducts.forEach(p => {
+                  if (p[field] == null) return;
+                  const key = fmtPower(p[field]);
+                  (byPower[key] = byPower[key] || 0);
+                  byPower[key]++;
+                });
+                const powerKeys = Object.keys(byPower).sort((a, b) => Number(a) - Number(b));
+                if (powerKeys.length) {
+                  powerChildren = powerKeys.map(key => {
+                    const powerId = `${brandId}::${key}`;
+                    const label = `${key} ${unit}`;
+                    nodeLabels[powerId] = label;
+                    return { id: powerId, label, count: byPower[key] };
+                  });
+                }
+              }
+
+              return { id: brandId, label: brand, count: brandProducts.length, children: powerChildren };
+            });
+          }
 
-    // ── Build & inject the dynamic sub-filter (brand / power) based on currentCat ──
-    function renderSubFilter() {
-      const wrap = document.getElementById('dynamicSubFilter');
-      if (!wrap) return;
+          return { id: cat, label, count: catProducts.length, children };
+        });
 
-      // Only inverters and panels currently have a sub-filter
-      if (currentCat !== 'inverters' && currentCat !== 'panels') {
-        wrap.hidden = true;
-        wrap.innerHTML = '';
-        return;
+        return { id: section.id, label: tr(section.i18n), count: catProducts_total(catNodes), children: catNodes };
+      }).filter(Boolean);
+    }
+    function catProducts_total(catNodes) { return catNodes.reduce((sum, n) => sum + n.count, 0); }
+
+    // ── Render the tree (re-run on every open/check/search change — cheap DOM diff via innerHTML) ──
+    function nodeMatchesSearch(node, q) {
+      if (!q) return true;
+      if (node.label.toLowerCase().includes(q)) return true;
+      return !!(node.children && node.children.some(c => nodeMatchesSearch(c, q)));
+    }
+    function findTreeNode(id, list) {
+      for (const n of (list || filterTree)) {
+        if (n.id === id) return n;
+        if (n.children) { const f = findTreeNode(id, n.children); if (f) return f; }
       }
+      return null;
+    }
+    function renderTreeNode(node, level, q) {
+      if (!nodeMatchesSearch(node, q)) return '';
+      const hasChildren = node.children && node.children.length;
+      const isOpen = openNodes.has(node.id) || (q && hasChildren);
+      const isChecked = selectedFilters.has(node.id);
+      const childIds = hasChildren ? node.children.map(c => c.id) : [];
+      const childSelected = childIds.filter(id => selectedFilters.has(id)).length;
+      const isIndeterminate = !isChecked && childSelected > 0 && childSelected < childIds.length;
+
+      const rowClasses = ['ftree-row'];
+      if (isChecked) rowClasses.push('checked', 'selected');
+      else if (isIndeterminate) rowClasses.push('indeterminate');
 
-      const inCat = allProducts.filter(p => p.category === currentCat);
-      const brands = [...new Set(inCat.map(p => p.brand).filter(Boolean))].sort();
+      const headingHtml = level === 1
+        ? `<p class="ftree-heading">${node.label}</p>`
+        : '';
 
-      let html = `
-        <div class="prod-subfilter-group">
-          <p class="prod-sidebar-label">${sl('brand')}</p>
-          <div class="prod-sub-radio-list" id="brandRadioList">
-            <label class="prod-sub-radio-item${currentBrand === 'all' ? ' active' : ''}" data-brand="all">
-              <span>${sl('allBrands')}</span>
-              <input type="radio" name="prodBrand" value="all" ${currentBrand === 'all' ? 'checked' : ''}>
-            </label>
-            ${brands.map(b => {
-              const count = inCat.filter(p => p.brand === b).length;
-              return `<label class="prod-sub-radio-item${currentBrand === b ? ' active' : ''}" data-brand="${escapeHtml(b)}">
-                <span>${escapeHtml(b)}</span>
-                <input type="radio" name="prodBrand" value="${escapeHtml(b)}" ${currentBrand === b ? 'checked' : ''}>
-              </label>`;
-            }).join('')}
-          </div>
+      const rowHtml = level === 1 ? '' : `
+        <div class="${rowClasses.join(' ')}">
+          <span class="ftree-toggle ${hasChildren ? '' : 'no-children'}" data-action="toggle" data-id="${node.id}">
+            <svg width="9" height="9" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><path d="M9 6l6 6-6 6"/></svg>
+          </span>
+          <span class="ftree-box" data-action="check" data-id="${node.id}">
+            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3.5"><path d="M5 13l4 4L19 7"/></svg>
+          </span>
+          <span class="ftree-label" data-action="check" data-id="${node.id}">${escapeHtml(node.label)}</span>
+          <span class="ftree-count">${node.count ?? ''}</span>
         </div>`;
 
-      // Power chips only for inverters, scoped to the selected brand (or all brands)
-      if (currentCat === 'inverters') {
-        const scoped = currentBrand === 'all' ? inCat : inCat.filter(p => p.brand === currentBrand);
-        const powers = [...new Map(
-          scoped.map(p => { const { kw } = parseInvPower(p); return [kw, kw]; }).filter(([kw]) => kw)
-        ).keys()].sort((a, b) => a - b);
-
-        html += `
-        <div class="prod-subfilter-group">
-          <p class="prod-sidebar-label">${sl('power')}</p>
-          <div class="prod-chip-list" id="powerChipList">
-            <button type="button" class="prod-chip${currentPower === 'all' ? ' active' : ''}" data-power="all">${sl('allPowers')}</button>
-            ${powers.map(kw => `<button type="button" class="prod-chip${String(currentPower) === String(kw) ? ' active' : ''}" data-power="${kw}">${kw} KW</button>`).join('')}
-          </div>
-        </div>`;
+      const childrenHtml = hasChildren
+        ? `<div class="ftree-children">${node.children.map(c => renderTreeNode(c, level + 1, q)).join('')}</div>`
+        : '';
+
+      // Level 1 = section heading (not selectable, just a divider + label).
+      // Level 2/3 = real filterable nodes (category / brand / power).
+      const openClass = level === 1 ? 'open' : (isOpen ? 'open' : '');
+      return `<div class="ftree-node ftree-lvl${level} ${openClass}" data-id="${node.id}">${headingHtml}${rowHtml}${childrenHtml}</div>`;
+    }
+    function renderTree() {
+      const el = document.getElementById('filterTree');
+      if (!el) return;
+      const q = (document.getElementById('ftreeSearch')?.value || '').trim().toLowerCase();
+      const html = filterTree.map(n => renderTreeNode(n, 1, q)).join('');
+      el.innerHTML = html || `<p class="ftree-empty">${tr('prod_tree_no_match')}</p>`;
+      renderChips();
+    }
+
+    function toggleOpen(id) {
+      if (openNodes.has(id)) openNodes.delete(id); else openNodes.add(id);
+      renderTree();
+    }
+    function toggleCheck(id) {
+      if (selectedFilters.has(id)) {
+        selectedFilters.delete(id);
+      } else {
+        selectedFilters.add(id);
+        openNodes.add(id); // reveal children immediately so the user can refine right away
       }
+      renderTree();
+      currentPage = 1;
+      renderGrid();
+    }
 
-      wrap.innerHTML = html;
+    // ── Active filter chips above the grid ──
+    function renderChips() {
+      const wrap = document.getElementById('activeChips');
+      if (!wrap) return;
+      if (selectedFilters.size === 0) { wrap.hidden = true; wrap.innerHTML = ''; return; }
       wrap.hidden = false;
-
-      // Wire up brand radios
-      wrap.querySelectorAll('#brandRadioList input[type="radio"]').forEach(input => {
-        input.addEventListener('change', () => {
-          const item = input.closest('.prod-sub-radio-item');
-          currentBrand = item ? item.dataset.brand : 'all';
-          currentPower = 'all'; // reset power when brand changes
-          renderSubFilter();
-          renderGrid();
-        });
-      });
-
-      // Wire up power chips
-      wrap.querySelectorAll('#powerChipList .prod-chip').forEach(chip => {
-        chip.addEventListener('click', () => {
-          currentPower = chip.dataset.power;
-          wrap.querySelectorAll('#powerChipList .prod-chip').forEach(c => c.classList.remove('active'));
-          chip.classList.add('active');
-          renderGrid();
-        });
-      });
+      const chips = Array.from(selectedFilters).map(id => `
+        <span class="active-chip">${escapeHtml(nodeLabels[id] || id)}
+          <button type="button" data-remove="${id}" aria-label="إزالة">✕</button>
+        </span>`).join('');
+      wrap.innerHTML = chips + `<button type="button" class="active-chip-clear" id="clearAllChips">${tr('prod_clear_all')}</button>`;
     }
 
-    document.querySelectorAll('#catRadioList input[type="radio"]').forEach(input => {
-      input.addEventListener('change', () => {
-        const item = input.closest('.prod-radio-item');
-        setActiveCategory(item ? item.dataset.cat : 'all');
-      });
+    document.getElementById('filterTree')?.addEventListener('click', (e) => {
+      const t = e.target.closest('[data-action]');
+      if (!t) return;
+      const id = t.getAttribute('data-id');
+      if (t.getAttribute('data-action') === 'toggle') toggleOpen(id); else toggleCheck(id);
+    });
+    document.getElementById('activeChips')?.addEventListener('click', (e) => {
+      if (e.target.id === 'clearAllChips') { selectedFilters.clear(); renderTree(); currentPage = 1; renderGrid(); return; }
+      const removeId = e.target.closest('[data-remove]')?.getAttribute('data-remove');
+      if (removeId) { selectedFilters.delete(removeId); renderTree(); currentPage = 1; renderGrid(); }
     });
+    document.getElementById('ftreeSearch')?.addEventListener('input', renderTree);
 
     // ── Price filter apply / reset ──
     document.getElementById('btnApplyFilter')?.addEventListener('click', () => {
@@ -1370,24 +1580,24 @@
       const toVal = document.getElementById('priceTo')?.value;
       priceMin = fromVal !== '' && fromVal != null ? parseFloat(fromVal) : null;
       priceMax = toVal !== '' && toVal != null ? parseFloat(toVal) : null;
+      currentPage = 1;
       renderGrid();
     });
 
     document.getElementById('btnResetFilter')?.addEventListener('click', () => {
       priceMin = null;
       priceMax = null;
-      currentBrand = 'all';
-      currentPower = 'all';
+      selectedFilters.clear();
+      openNodes.clear();
       const fFrom = document.getElementById('priceFrom');
       const fTo = document.getElementById('priceTo');
       if (fFrom) fFrom.value = '';
       if (fTo) fTo.value = '';
-      const allRadio = document.querySelector('.prod-radio-item[data-cat="all"]');
-      if (allRadio) {
-        const input = allRadio.querySelector('input[type="radio"]');
-        if (input) input.checked = true;
-      }
-      setActiveCategory('all');
+      const search = document.getElementById('ftreeSearch');
+      if (search) search.value = '';
+      renderTree();
+      currentPage = 1;
+      renderGrid();
     });
 
     // Allow Enter key inside price fields to trigger apply
@@ -1401,7 +1611,11 @@
     document.addEventListener('DOMContentLoaded', () => {
       document.querySelectorAll('[data-lang]').forEach(btn => {
         btn.addEventListener('click', () => {
-          setTimeout(() => { renderSubFilter(); renderGrid(); }, 50);
+          setTimeout(() => {
+            const search = document.getElementById('ftreeSearch');
+            if (search) search.placeholder = tr('prod_tree_search');
+            buildFilterTree(lastSectionOf); renderTree(); renderGrid();
+          }, 50);
         });
       });
     });
@@ -1444,10 +1658,10 @@
       }
 
       try {
-        const { data, error } = await sb
-          .from('products')
-          .select('*')
-          .order('sort_order', { ascending: true });
+        const [{ data, error }, { data: catSettings }] = await Promise.all([
+          sb.from('products').select('*').order('sort_order', { ascending: true }),
+          sb.from('category_settings').select('category_key, visible, main_section, sort_order'),
+        ]);
 
         loading.hidden = true;
 
@@ -1459,7 +1673,18 @@
           return;
         }
 
-        const rawProducts = data.filter(row => CATEGORIES.includes(row.category));
+        // Categories the admin switched off never make it into allProducts or
+        // the filter tree. A category with no row in category_settings
+        // defaults to visible so nothing disappears silently if the table is
+        // ever missing an entry.
+        const hiddenCats = new Set(
+          (catSettings || []).filter(c => c.visible === false).map(c => c.category_key)
+        );
+        const sectionOf = {};
+        (catSettings || []).forEach(c => { sectionOf[c.category_key] = c.main_section; });
+        lastSectionOf = sectionOf;
+
+        const rawProducts = data.filter(row => CATEGORIES.includes(row.category) && !hiddenCats.has(row.category));
 
         // Brand-level logo fallback: a product's own image_url always wins;
         // if it has none, fall back to the brand's shared logo (set once in
@@ -1476,7 +1701,8 @@
           ...p,
           image_url: p.image_url || (p.brand && brandLogos[p.brand]) || null,
         }));
-        renderSubFilter();
+        buildFilterTree(sectionOf);
+        renderTree();
         renderGrid();
       } catch (e) {
         console.error('loadProducts error:', e);
